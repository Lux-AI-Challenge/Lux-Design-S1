import { Game } from "../src/Game";

describe('test resource collection and distribution', () => {
  let game: Game;
  beforeEach(() => {
    game = new Game({
      width: 16,
      height: 16,
    });
  });
  it('should distribute evenly given all workers have space' () => {

  });
});